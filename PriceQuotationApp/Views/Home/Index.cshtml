@model PriceQuotationApp.Models.PriceQuotation

@{
    ViewData["Title"] = "Price Quotation";
}

<h2>Price Quotation</h2>

<form asp-action="Calculate" method="post">
    <div>
        <label for="Subtotal">Subtotal:</label>
        <input asp-for="Subtotal" type="text" />
        <span asp-validation-for="Subtotal" class="text-danger"></span>
    </div>
    <div>
        <label for="DiscountPercent">Discount Percent:</label>
        <input asp-for="DiscountPercent" type="text" />
        <span asp-validation-for="DiscountPercent" class="text-danger"></span>
    </div>
    <div>
        <button type="submit">Calculate</button>
        <a asp-action="Clear" asp-controller="PriceQuotation">Clear</a>
    </div>
    <div>
        <label>Discount Amount:</label>
        <span>@Model.DiscountAmount.ToString("C")</span>
    </div>
    <div>
        <label>Total:</label>
        <span>@Model.Total.ToString("C")</span>
    </div>
</form>
